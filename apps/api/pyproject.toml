[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "mypy>=1.19.1",
    "openai",
    "playwright",
    "pydantic",
    "pymongo",
    "requests",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "types-requests>=2.32.4.20260107",
]
